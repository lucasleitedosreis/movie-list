import React from 'react';
import { MenuBar } from '../../Components/MenuBar';
import { HeaderContainer } from './style';

export const Header = () => {
  return (
    <HeaderContainer>
      <MenuBar />
    </HeaderContainer>
  );
};
